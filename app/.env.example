# RunPod
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=
RUNPOD_API_BASE=https://api.runpod.ai/v2

# Google Drive: no credentials here. Ansible configures the rclone gdrive remote
# locally. Ensure `rclone listremotes` shows `gdrive:` before starting the app.
