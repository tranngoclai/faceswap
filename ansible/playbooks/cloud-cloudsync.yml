---
# Install the on-instance cron that auto-runs `vastai cloud copy` to Drive.
#   VAST_API_KEY=<scoped> ansible-playbook playbooks/cloud-cloudsync.yml
- name: Faceswap cloud-sync cron
  hosts: cloud
  gather_facts: false
  roles:
    - faceswap_cloudsync
