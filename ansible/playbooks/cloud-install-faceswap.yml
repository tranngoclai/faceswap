---
# Provision the vast.ai instance: clone + install deps + verify GPU.
#   ansible-playbook playbooks/cloud-setup.yml
- name: Faceswap cloud setup (install + check)
  hosts: cloud
  gather_facts: false
  roles:
    - faceswap_install
    - faceswap_check
