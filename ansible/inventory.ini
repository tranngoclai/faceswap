; Cloud GPU instance(s) for training. `ansible_host` is the ~/.ssh/config Host
; alias, so host/port/key live in ssh config (one place to update per rental).
[cloud]
vast ansible_host=vast-training ansible_user=root

; The control machine — runs Docker extract/convert and mints the vast API key.
[local]
localhost ansible_connection=local
